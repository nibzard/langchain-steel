"""
Backwards compatibility setup.py for langchain-steel.

This file is maintained for compatibility with older build tools and CI systems.
The primary build configuration is in pyproject.toml.
"""
from setuptools import setup

if __name__ == "__main__":
    setup()